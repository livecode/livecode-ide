switch tFoo
   case "foo"
      -- code
      break
   default
      -- code
      break
end switch

switch
   case tFoo begins with "foo"
      -- code
      break
   default
      -- code
      break
end switch
